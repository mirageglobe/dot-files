tasks:
  - name: Install, configure, and start Apache
    block:
      - name: task 1
        shell: |
          echo "test"
      - name: task 2
        shell: |
          echo "test 2"
    when: myvar == "enable"
    become: true
    become_user: root
    ignore_errors: yes
