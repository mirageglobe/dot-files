// === go

// === references
// - https://devhints.io/go

// === basic commands

// === === array

// === === class

// === === function

// === === if

// === === loop

// === === print

// === === regex

// === === switch

// === === variable

// === helper commands

// === === type conversions

// === === string functions

// === === numeric functions

// === === ranges short cut

// === === threads
