# this config should exist in as ~/.config/starship.toml
command_timeout = 3000

[aws]
symbol = ' '

[kubernetes]
disabled = false

[nim]
symbol = ' '

[nodejs]
format = 'via [ $version](bold green) '

[terraform]
format = 'via [ $version $workspace]($style) '
